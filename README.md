# THE UNKNOWN // CORE — Project 2
First working milestone: cinematic opening, CSS black hole, interactive particle field, Three.js starfield and responsive mobile layout.

GitHub Pages: upload `index.html`, `style.css`, `script.js` and keep the `modules` folder. Enable Settings → Pages → Deploy from `main` / root.

Note: Three.js is loaded from jsDelivr, so the 3D starfield needs internet access.
